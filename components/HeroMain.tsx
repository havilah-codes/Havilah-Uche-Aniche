'use client';

import HeroText from './HeroText';

const HeroMain = () => {
    return (
        <div id="home" className="relative overflow-hidden bg-slate-950">
            <HeroText />
        </div>
    )
}

export default HeroMain;